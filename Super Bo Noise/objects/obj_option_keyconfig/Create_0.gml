if (instance_number(object_index) > 1)
    instance_destroy();

selected = -2;
reading = false;
gamepad = false;
depth = -500;
inputs = ["up", "down", "left", "right", "jump", "slap", "taunt", "shoot", "attack", "superjump", "groundpound", "start"];
scroll_pos = 0;
scroll_pad = 50;
