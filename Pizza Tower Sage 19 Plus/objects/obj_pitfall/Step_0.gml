if (place_meeting(x, y, obj_doorA))
    targetDoor = "A";

if (place_meeting(x, y, obj_doorB))
    targetDoor = "B";

if (place_meeting(x, y, obj_doorC))
    targetDoor = "C";

if (place_meeting(x, y, obj_doorD))
    targetDoor = "D";

if (place_meeting(x, y, obj_doorE))
    targetDoor = "E";

if (place_meeting(x, y, obj_doorF))
    targetDoor = "F";

if (place_meeting(x, y, obj_doorG))
    targetDoor = "G";

if (place_meeting(x, y, obj_doorH))
    targetDoor = "H";

if (place_meeting(x, y, obj_doorI))
    targetDoor = "I";

if (place_meeting(x, y, obj_doorJ))
    targetDoor = "J";

if (place_meeting(x, y, obj_doorK))
    targetDoor = "K";

if (place_meeting(x, y, obj_doorL))
    targetDoor = "L";

if (place_meeting(x, y, obj_doorM))
    targetDoor = "M";

if (place_meeting(x, y, obj_doorN))
    targetDoor = "N";

if (place_meeting(x, y, obj_doorO))
    targetDoor = "O";
