x_move = Wave(-100, 100, durationspeed, 15);
y_move = Wave(-95, 95, durationspeedy, 15);

if (x_move >= 99)
    depth = gateid.depth - depthplus;

if (x_move <= -99)
    depth = gateid.depth + depthplus;
