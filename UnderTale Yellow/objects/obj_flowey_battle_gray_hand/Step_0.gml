if (live_call())
    return global.live_result;

if (abs(x - 320) > 900)
    instance_destroy();

sin_deg += sin_inc;

if (sin_deg > 360)
    sin_deg -= 360;

y = ystart + (sin(degtorad(sin_deg)) * y_variation);
