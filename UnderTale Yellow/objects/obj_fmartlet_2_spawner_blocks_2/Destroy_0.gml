if (live_call())
    return global.live_result;

with (obj_fmartlet_star_big)
    instance_destroy();

with (obj_martlet_attack_block_arrow_static)
    instance_destroy();

with (obj_dialogue_box_battle_transformation_any)
    event_user(0);
