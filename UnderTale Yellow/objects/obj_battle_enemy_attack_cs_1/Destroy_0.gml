if (live_call())
    return global.live_result;

with (obj_battle_enemy_attack_cactony_needle_top_generator_duo)
    instance_destroy();

with (obj_battle_enemy_attack_slither_snake_snake)
    instance_destroy();

with (obj_dialogue_box_battle_transformation_any)
    event_user(0);
