if (live_call())
    return global.live_result;

with (obj_battle_enemy_attack_cactony_needle_green_white)
    event_user(0);

with (obj_battle_enemy_attack_cactony_needle_green_white)
    event_user(1);

with (obj_battle_enemy_attack_cactony_needle_green_white)
    event_user(2);
