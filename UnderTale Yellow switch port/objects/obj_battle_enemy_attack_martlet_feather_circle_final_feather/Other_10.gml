if (live_call())
    return global.live_result;

x = spawner_id.frotate_x;
y = spawner_id.frotate_y;
image_angle = spawner_id.frotate_angle;

if (id == spawner_id.id_feather[0] || spawner_id.frotate_angle_use < angle_position_current)
    can_fade_in = true;

angle_position_current = spawner_id.frotate_angle_use;
