if (global.sworks_flag[57] == 0)
{
    if (global.fun_value >= 16 && global.fun_value <= 19)
        instance_destroy();
}

message[0] = "* (This security station would#  be useful to track roaming#  robots...)";
message[1] = "* (...if there were any cameras#  in the Steamworks.)";
