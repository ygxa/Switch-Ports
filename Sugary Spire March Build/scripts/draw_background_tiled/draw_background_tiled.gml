function draw_background_tiled(argument0, argument1, argument2) { // Feather disable all
	//i feel like a fucking valve programmer writer. my next line will certainly be "this is a stupid fix, but i don't have any ideas on how to do a cleaner fix."
	if global.panic = 0
	{
		draw_sprite_tiled(argument0, 0, argument1, argument2)
	}
	if global.panic = 1
	{
	}
	//what is this

}
