<<<<<<< HEAD
if global.shellactivate
	exit;

if ((!pause)  && (!instance_exists(obj_fadeout))) {
    if (obj_player.key_start && room != rank_room && room != realtitlescreen) && !global.shellactivate {
=======
if ((!pause) && (!instance_exists(obj_fadeout))) {
    if (obj_player.key_start && global.shellactivate == false && room != rank_room && room != realtitlescreen && canmove) {
>>>>>>> origin/(marioket)-super-freddy-fazbear-2
        selected = 0;
        if (!instance_exists(obj_pausefadeout)) 
			instance_create(x, y, obj_pausefadeout);
    }
}
if (instance_exists(obj_pausefadeout) && instance_exists(obj_fadeout)) instance_destroy(obj_pausefadeout);
if (pause == 1) {
    scr_getinput();
    application_surface_draw_enable(1);
	if canmove
	{
	    if (key_down2 && selected < 3) {
	        selected += 1;
	        scr_sound(sound_land);
	    }
	    if (key_up2 && selected > 0) {
	        selected -= 1;
	        scr_sound(sound_land);
	    }
	    if (key_jump) 
		{
			switch selected
			{
				case 0:
				if (!instance_exists(obj_pausefadeout)) //Exit
					instance_create(x, y, obj_pausefadeout);
				break;
				case 1:
				event_user(1); //Reset Level
				break;
				case 2:
				event_user(2); //Exit Level
				break;
				case 3:
				event_user(3); //Options Menu Pog
				break;
			}

		}
	}
}
if pause == 0 pal = obj_player.paletteselect;